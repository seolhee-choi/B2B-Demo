import { filter, find, findIndex, isEmpty, last, toString, isNil, trim, isString } from 'lodash'

export const _ = {
  isEmpty,
  filter,
  find,
  findIndex,
  last,
  toString,
  trim,
  isNil,
  isString
}
